#pragma once

void bubbleSort(char* scr, int w, int h, int* arr, int arrSize);

void selectionSort(char* scr, int w, int h, int* arr, int arrSize);

///void quickSort(char* scr, int w, int h, int* arr, int arrSize);

///int* mergeSort(char* scr, int w, int h, int* arr, int arrSize);